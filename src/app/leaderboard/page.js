import LeaderboardTemplate from "@/templates/LeaderboardTemplate";

export const metadata = {
  title: "Leaderboard on YoungParrot's Prediction",
  description: "YoungParrot's Prediction on Core blockchain",
};

export default function LeaderboardPage() {
  return <LeaderboardTemplate />;
}
