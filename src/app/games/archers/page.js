import ArchersGameTemplate from "@/templates/ArchersGameTemplate";

export const metadata = {
  title: "Archers Game",
  description: "Archers Game on Core blockchain",
};

const MintPage = () => {
  return (
    <div>
      <ArchersGameTemplate />
    </div>
  );
};

export default MintPage;
